<Window x:Name="window" x:Class="HelloWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Trigger Demo"
        Height="300"
        Width="450"
        MouseMove="Canvas_MouseMove"
        Background="yellow"
>

    <Window.Resources>
        <Style x:Key="NameBox" TargetType="TextBox">
            <Setter Property="Background" Value="AntiqueWhite"/>
            <Setter Property="Margin" Value="5 0 5 5"/>

            <Style.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter Property="Background" Value="Red" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0 20 20 0"/>
            <Setter Property="Opacity" Value="0.7"/>
            <Setter Property="Width" Value="100"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1" />
                    <Setter Property="FontSize" Value="15" />
                    <Setter Property="FontWeight" Value="Bold" />
                </Trigger>
            </Style.Triggers>

        </Style>
    </Window.Resources>

    <StackPanel x:Name="panel">

        <!--<Label Content="First Name" />
        <TextBox Style="{StaticResource NameBox}"/>
        <Label Content="Last Name" />
        <TextBox Style="{StaticResource NameBox}"/>-->
        <Button Style="{StaticResource ButtonStyle}" Content="RESET" Click="Button_Click" HorizontalAlignment="Center" />
        
        <Button x:Name="spawn" Style="{StaticResource ButtonStyle}" Content="?" Click="spawn_Button_Click" HorizontalAlignment="Center" />

        <TextBlock x:Name="txt" Margin="0,0,0,0" Text="" TextAlignment="center" Height="245"/>

        <Grid x:Name = "gridmap">
            
            
        </Grid>
        
        
        
    </StackPanel>
</Window>